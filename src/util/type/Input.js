////////////////////////////////////////
// Type
const NORMAL = 'normal';
const PASSWORD = 'password';
const LIST_TYPE = [
    NORMAL,
    PASSWORD
]

// KeyBoard
const KEYBOARD_DEFAULT = 'default';
const KEYBOARD_EMAIL_ADDRESS = 'email-address';
const KEYBOARD_NUMERIC = 'numeric';
const KEYBOARD_PHONE = 'phone-pad';
const KEYBOARD_NUMBER = 'number-pad';
const KEYBOARD_DECIMAL = 'decimal-pad';
const LIST_KEYBOARD = [
    KEYBOARD_DEFAULT,
    KEYBOARD_EMAIL_ADDRESS,
    KEYBOARD_NUMERIC,
    KEYBOARD_PHONE,
    KEYBOARD_NUMBER,
    KEYBOARD_DECIMAL
]

////////////////////////////////////////
export default {
    NORMAL, PASSWORD,
    LIST_TYPE,
    ////
    KEYBOARD_DEFAULT, KEYBOARD_EMAIL_ADDRESS, KEYBOARD_NUMERIC,
    KEYBOARD_PHONE, KEYBOARD_NUMBER, KEYBOARD_DECIMAL,
    LIST_KEYBOARD,
}