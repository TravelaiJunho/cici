import {StyleSheet} from 'react-native';

const s = StyleSheet.create({
    container: {
        alignItems: 'center',
    },
    vertical_layout: {
        flexWrap: 'wrap',
        flexDirection: 'row',
    },
    horizontal_layout: {
        flexDirection: 'row',
    },
});

export default s;
