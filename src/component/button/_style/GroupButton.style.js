import {StyleSheet} from 'react-native';

const s = StyleSheet.create({
    container_horizontal: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
    },
    container_vertical: {
        flex: 1,
        width: '100%',
    },
});

export default s;
