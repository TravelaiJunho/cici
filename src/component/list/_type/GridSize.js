////////////////////////////////////////

import Layout from '../../../util/Layout';

////////////////////////////////////////

export const SIZE_SINGLE = Layout.DEVICE_WIDTH / 3;
export const SIZE_MARGIN = Layout.UISize(2);
export const SIZE_MARGIN_HALF = SIZE_MARGIN / 2;
