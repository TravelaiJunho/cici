import {StyleSheet} from 'react-native';

const s = StyleSheet.create({
    container: {
        flex: 1,
    },
    image_layout: {
        height: '100%',
        width: '100%',
        resizeMode: 'cover',
    },
});

export default s;
